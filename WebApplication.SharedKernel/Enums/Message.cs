namespace WebApplication.SharedKernel.Enums
{
    public enum MessageType
    {
        Normal = 0,
        Request = 1,
        All = 2
    }

    public enum NotificationType
    {
        LastAvailableSession = 1,
        PaymentIsDone = 2
    }
}
