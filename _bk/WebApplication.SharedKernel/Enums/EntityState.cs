namespace WebApplication.SharedKernel.Enums
{
    public enum ActiveState
    {
        Deactive = 0,
        Active = 1
    }
    public enum DeleteState
    {
        Permanent = 0,
        SoftDelete = 1
    }
}
